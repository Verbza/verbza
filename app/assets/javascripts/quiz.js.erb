var QuizJS = {
  init: function() {
    $("#card_rectangle").on("click", function() {
      alert()
      var words = [gon.card.foreign_word, gon.card.native_word];
      var actual_word = words.shift();
      $("#card_word").show(actual_word);
      words.push(actual_word);
    });
  }
};

$(document).ready(function() {
  QuizJS.init();
});




// .on("click", this.toggleCard());
// },
//
// toggleCard: function() {
// alert()
// var words = [gon.card.foreign_word, gon.card.native_word];
// var actual_word = words.shift();
// $("#card_word").show(actual_word);
// words.push(actual_word);
// }
// };