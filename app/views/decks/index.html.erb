<h2><%= current_user.email %></h2>

<%= render 'layouts/search'%>

<div>
  <ul data-role="listview">
    <% @decks.each do |deck| %>
    <li class="list_bar"><%= link_to "#{deck.name}", deck_path(deck) %></li>
    <% end %>
  </ul>
</div>